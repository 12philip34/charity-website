import React from "react";

const Events = () => {

    return (
        <div>
            Events
            Events Events Events Events Events Events Events Events
        </div>
    )
}

export default Events;